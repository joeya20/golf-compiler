func main() {
    var i int;
    var j int;
    var k bool;
    var z bool;
    var a string;
    var b string;
    -k;
    // -k;
    i < j;
    // i+test(i);
    // true = k;
    // i = 20;
    // j = -i;
    // printi(test(10));
    return;
}

func test(k int) int {
    var i bool;
    if(i) {
        return 5;
    }
}


var k string;